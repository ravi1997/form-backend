FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
RUN pip install flake8 black mypy

COPY . .

CMD ["bash", "-c", "flake8 . && black --check . && mypy ."]
